import { rsocketBrokerClientJs } from './rsocket-broker-client-js';

describe('rsocketBrokerClientJs', () => {
  it('should work', () => {
    expect(rsocketBrokerClientJs()).toEqual('rsocket-broker-client-js');
  });
});
